# =============================================================================
# Salesforce Models API Gateway - Environment Configuration Template
# =============================================================================
# 
# CONFIGURATION PRECEDENCE (highest to lowest priority):
# 1. Environment Variables (export VARIABLE=value)
# 2. .env file (this file, automatically loaded by start scripts)  
# 3. .secure/config.json file (alternative configuration method)
# 4. Default values (hardcoded fallbacks)
#
# USAGE:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your values
# 3. Run: ./scripts/start_async_service.sh (automatically loads .env)
#
# =============================================================================

# Salesforce Authentication (REQUIRED)
# Get these from your Salesforce Connected App
SALESFORCE_CONSUMER_KEY=your_salesforce_consumer_key
SALESFORCE_CONSUMER_SECRET=your_salesforce_consumer_secret
SALESFORCE_USERNAME=your_salesforce_username
SALESFORCE_INSTANCE_URL=https://yourcompany.my.salesforce.com
SALESFORCE_API_VERSION=v64.0

# Tool Calling Configuration (Optional)
# Controls how function calls are handled and validated
TOOL_CALLING_ALLOW_DANGEROUS_FUNCTIONS=false
TOOL_CALLING_STRICT_VALIDATION=true
TOOL_CALLING_MAX_CONCURRENT=3
TOOL_CALLING_TIMEOUT=30.0

# Server Configuration (Optional)
# Basic server settings - defaults work for most cases
HOST=0.0.0.0
PORT=8000
DEBUG=false
ENVIRONMENT=development
MAX_WORKER_MEMORY=512
VERBOSE_TOOL_LOGS=false

# Compatibility Configuration (Optional)
# Enable specific compatibility modes for different clients
N8N_COMPAT_MODE=1                    # Enable n8n/OpenAI.js compatibility
OPENAI_NATIVE_TOOL_PASSTHROUGH=1     # Allow OpenAI-style tool calls
N8N_COMPAT_PRESERVE_TOOLS=1          # Preserve tools for n8n clients  
NATIVE_ANTHROPIC_ENABLED=false       # Enable legacy Anthropic router
OPENAI_FRONTDOOR_ENABLED=0           # Enable OpenAI front-door architecture
SF_RESPONSE_DEBUG=false              # Enable detailed response logging

# Performance & Advanced Features (Optional)
# Production deployment settings
WORKERS=4                            # Number of gunicorn workers