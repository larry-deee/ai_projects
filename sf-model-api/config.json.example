{
    // ============================================================================
    // SALESFORCE MODELS API GATEWAY - LOCAL PROTOTYPE CONFIGURATION
    // ============================================================================
    // This configuration is for LOCAL DEVELOPMENT ONLY - not production use.
    // Only parameters actually used by the codebase are included.
    
    // ----------------------------------------------------------------------------
    // REQUIRED: Salesforce Connected App Authentication
    // ----------------------------------------------------------------------------
    // These 3 parameters are MANDATORY - server will fail to start without them
    // Get these from your Salesforce Connected App configuration
    
    "consumer_key": "YOUR_SALESFORCE_CONNECTED_APP_CONSUMER_KEY",
    "consumer_secret": "YOUR_SALESFORCE_CONNECTED_APP_CONSUMER_SECRET", 
    "instance_url": "https://your-instance.my.salesforce.com",
    
    // ----------------------------------------------------------------------------
    // OPTIONAL: API Configuration
    // ----------------------------------------------------------------------------
    // These have sensible defaults if omitted
    
    "api_version": "v64.0",
    // Default: "v64.0" - Salesforce API version to use
    
    "token_file": "salesforce_models_token.json",
    // Default: "salesforce_models_token.json" - Where to cache OAuth tokens
    
    // ----------------------------------------------------------------------------
    // TOOL CALLING SECURITY (Local Prototype Settings)
    // ----------------------------------------------------------------------------
    // WARNING: These settings are for LOCAL DEVELOPMENT only
    // Production deployments should use much more restrictive settings
    
    "tool_calling": {
        "allow_dangerous_functions": false,
        // SECURITY: Set to false for local prototype safety
        // When true, enables system commands and file operations (CVSS 9.8 risk)
        
        "strict_parameter_validation": true,
        // Recommended: Enforce strict validation of tool parameters
        
        "max_concurrent_calls": 3,
        // Limit simultaneous tool executions to prevent resource exhaustion
        
        "timeout": 30.0
        // Tool execution timeout in seconds (prevents hanging operations)
    }
    
    // ============================================================================
    // REMOVED PARAMETERS (Not used by current codebase):
    // ============================================================================
    // The following parameters from previous versions are NOT read by the code:
    // 
    // • "username" - Not used in Client Credentials OAuth flow
    // • "default_model" - Code hardcodes "claude-3-haiku" default
    // • "default_max_tokens" - Code hardcodes 1000 default  
    // • "default_temperature" - Code hardcodes 0.7 default
    // • "oauth_timeout" - Not implemented in current OAuth logic
    // • "token_buffer_seconds" - Not used in token refresh logic
    // • "max_oauth_retries" - Not implemented in error handling
    // • "connection_pool_size" - Pool size handled by connection_pool.py defaults
    // • "keep_alive_timeout" - Connection timeouts handled by HTTP client defaults
    // • "security_profile" - Not used in tool calling logic
    // • "enable_write_operations" - Dangerous and not implemented safely
    // • "write_operation_whitelist" - Complex whitelist not used
    // • "allowed_dangerous_functions" - Security risk, not implemented
    // • "blocked_functions" - Not used in current filtering logic
    //
    // Model, token, and temperature defaults are set in the request handlers:
    // • async_endpoint_server.py:634-636 
    // • llm_endpoint_server.py:1429-1431
    // ============================================================================
}